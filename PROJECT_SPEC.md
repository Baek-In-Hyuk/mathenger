# Mathenger 수학 교육 AI 프로그램
## 프로젝트 전체 인수인계 문서 / 개발 명세서

**문서 목적:**  
이 문서는 지금까지 기획자가 ChatGPT와 장기간 논의하면서 확정한 Mathenger 수학 교육 프로그램의 목표, 개발 방향, 데이터 구조, UI, OCR, HWPX, 학생 학습 시스템, 권한 체계, 서버 비용 절감 방식, 결정사항, 보류사항 및 향후 개발 계획을 다른 AI 또는 개발자가 그대로 이어받을 수 있도록 정리한 것이다.

---

# 1. 프로젝트의 최종 목표

이 프로젝트의 최종 목표는 단순한 학원용 문제관리 프로그램이 아니다.

**중·고등학교에서 실제 부교재 또는 학습 시스템으로 채택될 수 있는 수준의 수학 교육 플랫폼을 만드는 것**이 최종 목표이다.

초기에는 개인 학원에서 사용할 수 있는 문제 관리 및 학습 시스템으로 시작하지만, 장기적으로는 여러 학원과 학교에서 사용할 수 있도록 확장 가능하게 설계한다.

따라서 처음부터 특정 학원에서만 사용할 수 있는 임시 프로그램으로 만들지 않는다.

핵심 원칙은 다음과 같다.

> **작게 시작하되, 나중에 학교/출판/대규모 학생 데이터를 처리할 수 있도록 처음부터 확장 가능한 구조로 만든다.**

---

# 2. 개발 철학

이 프로젝트에서 가장 중요한 개발 철학은 다음과 같다.

## 2.1 처음부터 확장성을 막지 않는다.

학생 성향, 문제 유형, 교육과정, 해설 종류, OCR 분류 등은 처음부터 몇 개의 고정된 항목으로 막아버리지 않는다.

처음에는 기본적인 분류체계로 시작하되 실제 데이터가 쌓이면 새로운 분류를 추가할 수 있어야 한다.

특히 학생 성향 분류는 다음과 같이 만든다.

잘못된 방식:

```text
학생 성향
① 계산형
② 개념형
③ 응용형
④ 암기형
```

이렇게 4개 또는 5개로 고정하지 않는다.

올바른 방향:

```text
초기 분류
↓
학생 학습 데이터 축적
↓
오답/풀이시간/힌트사용/재풀이/풀이패턴 분석
↓
새로운 특성 발견
↓
분류체계 확장
```

즉, **데이터를 관찰하면서 발전할 수 있는 구조**여야 한다.

---

# 3. 시스템 전체 구조

전체 시스템은 크게 다음과 같이 구성한다.

```text
                    Mathenger
                       │
        ┌──────────────┼──────────────┐
        │              │              │
     관리자         선생님           학생
        │              │              │
        └──────────────┼──────────────┘
                       │
                    중앙 서버
                       │
       ┌───────────────┼────────────────┐
       │               │                │
      DB            파일 저장소       AI/OCR 서버
       │               │                │
       └───────────────┼────────────────┘
                       │
                학생 태블릿/PC
                       │
                   로컬 캐시
```

단, 모든 작업을 서버에서 처리하지 않는다.

---

# 4. 서버 비용 절감이 기본 설계 원칙이다.

프로그램이 커졌을 때 가장 큰 문제 중 하나는 서버 비용이다.

따라서 다음 구조를 기본으로 한다.

## 4.1 로컬 캐시

학생이 문제를 한 번 다운로드하면 태블릿에 저장한다.

같은 문제를 다시 볼 때마다 서버에서 다시 다운로드하지 않는다.

예:

```text
문제 ID = P000123

최초 접근
학생 태블릿 ← 서버

이후 반복 학습
학생 태블릿 → 로컬 문제 사용
```

---

## 4.2 문제 ID 중심 관리

문제를 학생마다 복제하지 않는다.

DB에는 하나의 문제를 저장하고 학생은 문제 ID를 참조한다.

```text
Problem
P000123
```

학생의 학습기록에는:

```text
student_id
problem_id
attempt_id
result
timestamp
```

등을 저장한다.

---

## 4.3 다시 풀 문제

학생이 문제를 저장하는 기능을 제공한다.

그러나 문제 자체를 학생 계정에 복제하지 않는다.

```text
학생이 저장
↓
problem_id 저장
↓
태블릿에 문제 콘텐츠가 이미 존재하면 로컬에서 불러옴
↓
없으면 최초 1회 다운로드
↓
이후 오프라인에서도 확인 가능
```

즉, **문제 ID + 로컬 캐시** 방식이다.

---

# 5. 오프라인 학습

학생 태블릿에서 인터넷이 끊겨도 학습할 수 있어야 한다.

특히 중요한 것은 학생이 문제를 풀면서 기록하는 **Stroke 데이터**이다.

인터넷이 끊겼다고 학생의 필기 데이터가 사라지면 안 된다.

따라서:

```text
학생 필기
↓
태블릿 로컬 저장
↓
인터넷 없음 → 계속 학습 가능
↓
인터넷 복구
↓
변경된 데이터만 서버 동기화
```

방식을 사용한다.

---

# 6. Stroke 저장

학생이 태블릿에서 문제를 풀 때 단순히 최종 이미지만 저장하는 것이 아니라 가능한 한 **Stroke 단위의 원본 필기 데이터**를 저장한다.

예:

```text
stroke_id
student_id
problem_id
timestamp
x
y
pressure
time
tool
```

등을 활용한다.

이를 통해 장기적으로 다음이 가능해진다.

- 필기 분석
- 풀이 과정 분석
- 학생별 필체 학습
- OCR 개선
- 풀이 습관 분석
- AI 기반 학습 분석

단, 모든 Stroke를 서버로 실시간 전송하지 않는다.

**로컬 저장 → 압축 → 변경분만 배치 동기화**를 기본으로 한다.

---

# 7. 학생 학습 기록

학생은 자신의 학습 내역을 다시 볼 수 있어야 한다.

학생 화면에서 최소한 다음 정보를 제공한다.

- 학습한 문제
- 맞힌 문제
- 틀린 문제
- 다시 풀고 싶은 문제
- 학습 진도
- 앞으로 나갈 진도
- 단원별 학습 현황
- 필요에 따라 풀이/해설 확인

학생이 자신의 학습 상황을 스스로 확인할 수 있어야 한다.

---

# 8. 교육과정 DB

이 부분은 **초기 DB 설계에서 가장 중요하게 고려해야 하는 부분 중 하나**이다.

교육과정은 영구적으로 고정된 것이 아니다.

예를 들어 현재는:

```text
중1
중2
중3
고1
고2
```

를 기준으로 구성하지만 교육과정이 변경될 수 있다.

따라서 DB를 다음처럼 단순한 고정 테이블로 만들지 않는다.

```text
중1_수학_대단원1
중1_수학_대단원2
...
```

이런 방식은 교육과정이 변경될 때 프로그램 전체를 뜯어고쳐야 하는 문제가 생긴다.

---

# 9. 권장 교육과정 구조

교육과정은 계층 구조로 관리한다.

```text
교육과정
 └─ 학년
     └─ 과목
         └─ 대단원
             └─ 중단원
                 └─ 소단원
                     └─ 유형
                         └─ 관련 공식
```

각 항목은 ID를 가진다.

예:

```text
curriculum_id
grade_id
subject_id
chapter_id
unit_id
subunit_id
type_id
formula_id
```

문제는 이름을 직접 저장하기보다 가능한 한 해당 ID를 참조한다.

---

# 10. 교육과정 변경 대응

교육과정이 바뀌었을 때:

- 추가
- 삭제
- 이름 변경
- 단원 이동
- 유형 추가
- 공식 추가

등이 가능해야 한다.

따라서 **교육과정 데이터를 코드에 하드코딩하지 않는다.**

관리자가 DB에서 수정할 수 있도록 만든다.

이를 통해 새로운 교육과정이 나와도 프로그램 자체를 다시 개발하지 않고 데이터만 추가/수정할 수 있도록 한다.

---

# 11. 문제 DB

문제 DB는 프로젝트의 핵심이다.

문제 하나는 최소한 다음과 같은 관계를 가진다.

```text
Problem
 ├─ problem_id
 ├─ curriculum_id
 ├─ chapter_id
 ├─ unit_id
 ├─ subunit_id
 ├─ type_id
 ├─ difficulty
 ├─ source
 ├─ page
 ├─ problem_number
 ├─ content
 ├─ formulas
 ├─ figures
 ├─ choices
 ├─ answer
 ├─ explanations
 └─ hints
```

실제 DB에서는 이를 하나의 거대한 테이블에 모두 넣기보다 관계를 분리하는 방향으로 설계한다.

---

# 12. 왜 초기 DB 설계가 중요한가

이 프로젝트에서 **초기 DB 설계를 잘하는 것은 매우 중요하다.**

이유는 나중에 저장해야 할 데이터가 계속 늘어나기 때문이다.

처음에는:

```text
문제
정답
해설
```

만 필요해 보일 수 있다.

하지만 실제 개발을 진행하면 다음이 추가된다.

```text
문제
교육과정
유형
공식
힌트
여러 개의 해설
AI 해설
선생님 해설
학생 풀이
Stroke
OCR 결과
OCR confidence
문제 이미지
그림
문제 버전
교육과정 버전
학생 학습기록
오답기록
재풀이 기록
학생 성향 데이터
```

초기 DB가 잘못 설계되면 나중에 데이터 구조를 전부 바꾸어야 한다.

따라서 **처음부터 모든 기능을 완성할 필요는 없지만, 데이터 간 관계를 확장할 수 있도록 설계해야 한다.**

특히 다음을 피한다.

```text
문제1_해설1
문제1_해설2
문제1_힌트1
문제1_힌트2
```

같은 고정된 구조.

대신:

```text
Problem
   ↓
Explanation
   ├─ explanation_id
   ├─ problem_id
   ├─ type
   ├─ title
   ├─ content
   └─ order

Hint
   ├─ hint_id
   ├─ problem_id
   ├─ level
   └─ content
```

처럼 **1:N 관계**로 설계한다.

이렇게 해야 해설을 1개에서 5개, 10개로 늘려도 DB 구조를 다시 만들 필요가 없다.

---

# 13. 여러 개의 나만의 해설

선생님이 직접 만든 풀이를 여러 개 저장할 수 있어야 한다.

예:

```text
문제 P000123

해설 1
→ 정석 풀이

해설 2
→ 기하적 풀이

해설 3
→ 학생에게 쉽게 설명하는 풀이

해설 4
→ 다른 관점의 풀이
```

따라서 DB에는 처음부터 **여러 해설을 저장할 수 있는 구조를 만들어야 한다.**

UI에도:

```text
[+ 나만의 풀이 추가]
```

버튼을 제공한다.

---

# 14. 해설 작성 방식

해설은 최소 두 가지 입력 방식을 고려한다.

### 방식 1. HWPX

선생님이 한컴오피스/HWPX 방식으로 풀이를 작성하여 저장할 수 있다.

### 방식 2. 태블릿 필기

선생님이 태블릿에서 직접 풀이하고 업로드할 수 있다.

따라서 해설 데이터는 단순 텍스트만을 저장하는 구조로 만들지 않는다.

---

# 15. 힌트 DB

해설관리에는 **힌트** 개념을 별도로 만든다.

힌트는 정답을 바로 알려주는 것이 아니라 학생이 다음 단계로 진행할 수 있도록 돕는 자료이다.

예:

```text
Hint 1
→ 어떤 공식을 사용할지 생각해 보세요.

Hint 2
→ 두 점의 관계를 먼저 찾아보세요.

Hint 3
→ 이 식을 인수분해해 보세요.
```

힌트도 처음부터 DB에 별도 구조로 만든다.

```text
Hint
 ├─ hint_id
 ├─ problem_id
 ├─ level
 ├─ content
 └─ order
```

힌트가 1개에서 여러 개로 늘어나도 DB 구조를 변경하지 않는다.

---

# 16. PDF 문제 업로드

선생님은 PDF를 업로드할 수 있어야 한다.

PDF에는 다음과 같은 구조가 있을 수 있다.

```text
앞쪽
문제 1
문제 2
문제 3
...

뒤쪽
해설 1
해설 2
해설 3
...
```

따라서 단순히 페이지 순서만 보고 문제와 해설을 연결하면 안 된다.

문제 번호, 페이지, 문서 구조, 해설 번호 등을 종합하여 **문제-해설 매칭 시스템**을 만든다.

AI가 자동 매칭하되 작업자가 확인/수정할 수 있도록 한다.

---

# 17. HWPX 문제 업로드의 특수성

HWPX에서는 PDF와 달리 문제집 이름이나 페이지 정보가 문서 내부에 명확하게 포함되어 있지 않을 수 있다.

따라서 업로드 시 필요한 메타데이터를 별도로 입력할 수 있어야 한다.

예:

```text
교재명
출판사
교육과정
학년
과목
페이지
문제 번호
문제 유형
```

필요한 경우 작업자가 직접 입력/수정한다.

---

# 18. HWPX 미주 문제

HWPX 문제집에는 문제마다 해설이 **미주** 형태로 들어 있을 수 있다.

이 경우 단순 텍스트 추출을 하면 문제가 섞이거나 번호가 깨질 가능성이 있다.

따라서 HWPX를 분석할 때는 단순 OCR보다 **HWPX 내부 XML 구조를 이용하여 문제 본문과 미주 관계를 분석하는 방식**을 우선한다.

문제 번호가 깨지는 경우에도 번호에만 의존하지 않고:

- 문서 순서
- XML 관계
- 미주 연결 정보
- 문제 영역
- 텍스트
- 수식
- 페이지/위치

등을 종합해 매칭한다.

---

# 19. HWPX ↔ JSON 수식 구조

프로젝트에서 매우 중요한 기술 영역이다.

목표는:

```text
HWPX
 ↓
수식 추출
 ↓
JSON
 ↓
JSON 수정/생성
 ↓
HWPX 수식
```

이 가능하도록 만드는 것이다.

단순 문자열만 저장하는 것이 아니라 장기적으로는 수식의 구조를 표현할 수 있도록 한다.

예:

```json
{
  "type": "fraction",
  "numerator": "...",
  "denominator": "..."
}
```

또는

```json
{
  "type": "superscript",
  "base": "x",
  "exponent": "2"
}
```

등의 구조를 사용할 수 있다.

---

# 20. HWPX 기술 검증 결과

실제 테스트 파일을 대상으로 다음 작업을 수행했다.

```text
HWPX
 ↓
HWPX 내부 수식 추출
 ↓
JSON 저장
 ↓
JSON에서 수식 복원
 ↓
HWPX 재생성
```

테스트에서는 **60개의 수식 스크립트를 추출 후 다시 HWPX로 복원했고 원본 수식 스크립트와 동일함을 검증했다.**

또한 재생성한 HWPX에서 문제 배치와 기존 그림의 위치도 정상적으로 유지되는 것을 확인했다.

단, 이 테스트는 그림을 새로 그린 것이 아니다.

원본 HWPX의 그림과 문서 구조를 유지하면서 수식 부분을 복원한 것이다.

---

# 21. 최종 HWPX 목표

장기적으로는 다음이 가능해야 한다.

```text
문제 데이터
   ↓
JSON
   ├─ 텍스트
   ├─ 수식
   ├─ 그림
   ├─ 표
   └─ 위치 정보
          ↓
      HWPX 생성
          ↓
한컴오피스에서 정상 표시
```

특히 수식은 우리 시스템의 JSON을 이용하여 HWPX 수식 객체로 생성할 수 있어야 한다.

이 부분은 개발 초기 별도 프로토타입으로 충분히 검증한다.

---

# 22. OCR의 기본 방향

OCR은 하나의 엔진에 의존하지 않는다.

**여러 공개 OCR 모델/엔진을 조합하는 방식**을 사용한다.

기본적인 방향:

```text
PDF / 이미지
      ↓
문서 영역 분석
      ↓
 ┌────┼────┐
 ↓    ↓    ↓
문자  수식  그림
 ↓    ↓    ↓
일반  수학  이미지
OCR   OCR   처리
 └────┼────┘
      ↓
문서 구조 통합
      ↓
우리의 JSON
```

---

# 23. OCR에서 고려하는 공개 엔진

초기 후보에는 다음과 같은 계열을 고려한다.

- Tesseract
- CRAFT 계열
- Pix2Text
- pix2tex 계열
- 기타 공개 수학 OCR 모델
- 필요에 따라 다른 공개 OCR 모델

단, 특정 모델 하나를 영구적으로 고정하지 않는다.

실제 PDF 테스트 결과를 보고 엔진을 교체하거나 추가할 수 있어야 한다.

---

# 24. Tesseract의 역할

Tesseract는 모든 수식을 해결하는 엔진으로 사용하지 않는다.

주 역할은:

- 한글
- 영어
- 일반 숫자
- 일반 문자

등의 텍스트 인식이다.

수학식은 별도의 수학 OCR 모델을 사용한다.

---

# 25. 수학 OCR

수학 OCR은 일반 OCR과 별도로 처리한다.

예:

```text
분수
지수
첨자
근호
시그마
적분
연립방정식
행렬
여러 줄 수식
```

등을 구조적으로 인식해야 한다.

목표는 단순히:

```text
x2+1/x
```

를 얻는 것이 아니라,

```text
fraction
 ├─ numerator
 └─ denominator
```

처럼 수식 구조를 이해하는 것이다.

---

# 26. OCR은 처음부터 완벽할 필요가 없다.

프로젝트의 핵심 전략은:

```text
공개 OCR
↓
실제 문제 적용
↓
오류 발생
↓
선생님 수정
↓
수정 결과 저장
↓
학습 데이터 축적
↓
모델 개선
```

이다.

즉, **처음부터 우리만의 거대한 OCR 모델을 만들지 않는다.**

공개 모델을 조합하여 시작하고 실제 사용 데이터로 점점 발전시킨다.

---

# 27. OCR 서버의 장기 발전

OCR 서버는 학습을 통해 발전 가능한 구조로 만든다.

단순히:

```text
PDF → OCR → 끝
```

으로 만들지 않는다.

다음 정보를 저장할 수 있도록 한다.

```text
원본 이미지
OCR 결과
모델 버전
confidence
사람 수정 결과
최종 정답
```

이를 이용해 나중에 학습 데이터셋을 자동 구축한다.

---

# 28. 학생 필기 OCR

학생이 태블릿에서 문제를 풀면 Stroke 데이터가 쌓인다.

이 데이터는 단순 이미지보다 가치가 높다.

```text
학생 Stroke
↓
필기 데이터
↓
수식/문자 분석
↓
OCR
↓
학생별 필체 특성
```

장기적으로 학생 개인의 필체를 반영하는 OCR로 발전시키는 것도 고려한다.

---

# 29. OCR의 최종 방향

궁극적으로는:

```text
공개 OCR
+
수학 OCR
+
레이아웃 분석
+
문제 구조 분석
+
우리 데이터
+
선생님 검수 데이터
+
학생 Stroke
```

를 조합하여 **Mathenger만의 OCR 시스템**으로 발전시킨다.

---

# 30. 학생 성향 분석

학생 성향은 처음부터 제한된 카테고리로 고정하지 않는다.

학생 데이터를 지속적으로 관찰한다.

예:

- 문제 풀이 시간
- 오답률
- 같은 유형 반복 오답
- 힌트 사용
- 재풀이 횟수
- 풀이 과정
- 풀이 중단
- 계산 실수
- 개념 오류
- 문제 읽기 오류
- 특정 단원 취약성

등을 데이터화한다.

이를 통해 장기적으로 학생별 학습 특성을 분석한다.

---

# 31. 문제 업로드 및 작업자 시스템

다른 선생님도 문제를 업로드할 수 있어야 한다.

그러나 아무 문제나 즉시 학생에게 출제되면 안 된다.

기본 흐름:

```text
선생님/작업자
       ↓
문제 업로드
       ↓
AI 자동 분석
       ↓
유형 분류
       ↓
교육과정 연결
       ↓
해설 연결
       ↓
개념 연결
       ↓
힌트 생성/등록
       ↓
작업자 수정
       ↓
마스터 최종 검토
       ↓
승인
       ↓
학생에게 사용 가능
```

---

# 32. 작업자도 수정할 수 있어야 한다.

작업자는 단순 업로드만 하는 사람이 아니다.

작업자에게도 필요한 편집 기능을 제공한다.

예:

- 문제 내용 수정
- 문제 번호 수정
- 교육과정 수정
- 유형 수정
- 해설 수정
- 개념 연결 수정
- 힌트 수정
- OCR 오류 수정
- 메타정보 수정

단, 최종 공개/사용 승인은 마스터가 담당한다.

---

# 33. 권한 체계

기본 권한은 다음 3단계로 구성한다.

## 마스터

최고 권한.

- 전체 문제 관리
- 교육과정 관리
- DB 관리
- 선생님 관리
- 학생 관리
- 최종 승인
- 삭제
- 시스템 설정
- AI/OCR 관리

## 선생님/작업자

- 문제 업로드
- 문제 수정
- 메타정보 입력
- 유형 분류
- 해설 입력
- 힌트 입력
- OCR 수정
- 검수 작업

단, 최종 승인 권한은 마스터가 가진다.

## 학생

- 문제 풀이
- 풀이 기록
- 제출
- 오답 확인
- 다시 풀 문제 저장
- 해설/힌트 확인
- 학습 진도 확인
- 자신의 학습 기록 확인

---

# 34. 학생 문제 저장

학생이 나중에 다시 풀고 싶은 문제를 체크할 수 있어야 한다.

중요한 원칙:

**학생마다 문제 파일을 복사하지 않는다.**

```text
학생 계정
↓
saved_problem
↓
problem_id
```

만 저장한다.

문제 콘텐츠는 중앙 DB와 로컬 캐시를 활용한다.

---

# 35. 학생 UI의 기본 방향

학생 화면은 복잡하지 않아야 한다.

기본 메뉴:

```text
[오늘의 학습]

[학습하기]

[틀린 문제]

[다시 풀 문제]

[학습 기록]

[진도]

[앞으로 학습할 내용]
```

문제를 풀 때는:

```text
문제
──────────────

학생 필기 영역

──────────────

[힌트]
[제출]
[다시 풀기]
[저장]
```

형태를 기본으로 한다.

---

# 36. 선생님 UI의 기본 방향

선생님 화면은 관리 중심이다.

```text
대시보드

문제 관리
 ├─ 문제 업로드
 ├─ 문제 검수
 ├─ 문제 수정
 └─ 문제 검색

교육과정 관리

해설 관리

힌트 관리

학생 관리

학습 분석

OCR 관리

승인 대기
```

---

# 37. 문제 메타정보 UI

문제 업로드 후 다음 정보를 입력/수정할 수 있어야 한다.

```text
교재명
출판사
교육과정
학년
과목
대단원
중단원
소단원
유형
난이도
페이지
문제번호
정답
```

AI가 자동으로 입력할 수 있지만 작업자가 수정할 수 있어야 한다.

---

# 38. 문제 출제 UI

선생님이 조건을 선택하여 문제를 출제할 수 있어야 한다.

예:

```text
학년: 고1
과목: 공통수학
대단원: ...
중단원: ...
소단원: ...
유형: ...
난이도: 중
문제 수: 10
```

등을 선택한다.

향후에는 학생별 데이터를 이용하여 자동으로 문제를 추천할 수 있도록 확장한다.

---

# 39. 문제 버전 관리

문제는 나중에 수정될 수 있다.

따라서 문제 ID와 버전을 분리한다.

예:

```text
problem_id = P000123

version 1
version 2
version 3
```

학생이 어떤 버전으로 풀었는지도 추적할 수 있어야 한다.

---

# 40. 원본 데이터 보존

OCR 또는 AI가 원본을 변경해버리면 안 된다.

가능한 한:

```text
원본
↓
AI 분석본
↓
사람 수정본
↓
최종 승인본
```

을 분리한다.

원본 파일은 보존하고 수정 결과는 별도로 관리하는 방향을 사용한다.

---

# 41. 데이터 보존 등급

모든 데이터를 똑같이 영구 저장하면 서버 비용이 커진다.

따라서 데이터 보존 등급을 만든다.

예:

```text
Tier 1
핵심 문제/정답/교육과정
→ 장기 보존

Tier 2
학생 학습 기록
→ 장기 보존

Tier 3
세부 Stroke
→ 필요 기간 보존 + 압축

Tier 4
임시 OCR 중간 결과
→ 일정 기간 후 정리 가능
```

구체적인 기간은 개발 단계에서 결정한다.

---

# 42. AI 사용 비용 절감

모든 요청을 AI 서버에 보내지 않는다.

기본 구조:

```text
학생 태블릿
↓
가능한 작업은 로컬 처리
↓
일반 서버
↓
필요할 때만 AI 서버
```

예를 들어 단순한 문제 표시나 저장 문제 때문에 AI를 호출하지 않는다.

AI는:

- OCR
- 수식 분석
- 복잡한 학습 분석
- 개인화
- 고급 문제 생성

등 **정말 필요한 작업에서만 사용한다.**

---

# 43. 데이터 동기화

전체 데이터를 매번 서버와 주고받지 않는다.

**Delta Sync**, 즉 변경된 부분만 동기화하는 구조를 사용한다.

```text
태블릿
현재 version = 10

서버
현재 version = 12

→ version 11~12에서 변경된 데이터만 다운로드
```

같은 방식으로 설계한다.

---

# 44. 대용량 파일 관리

문제 이미지/HWPX/PDF 등을 DB에 직접 중복 저장하지 않는다.

가능한 경우:

```text
DB
↓
file_id
↓
파일 저장소
```

방식으로 관리한다.

이를 통해 DB가 불필요하게 커지는 것을 방지한다.

---

# 45. 로컬 캐시 정책

학생 태블릿은 자주 사용하는 문제를 로컬에 저장한다.

예:

```text
최근 학습 문제
저장한 문제
현재 학습 과정
최근 해설
```

등을 우선 캐시한다.

캐시가 가득 차면 오래 사용하지 않은 자료부터 삭제할 수 있다.

단, 삭제되어도 서버에서 다시 받을 수 있어야 한다.

---

# 46. 현재까지 확정된 핵심 결정사항

다음 사항은 현재 프로젝트 방향으로 **확정**한다.

1. 최종 목표는 중·고등학교 부교재/학습 시스템으로 확장하는 것이다.
2. 처음부터 확장 가능한 구조로 만든다.
3. 학생 성향 분류를 고정된 몇 가지로 제한하지 않는다.
4. 실제 데이터 관찰을 통해 성향 분석 체계를 발전시킨다.
5. 교육과정 DB를 학년→과목→대단원→중단원→소단원→유형→공식 구조로 관리한다.
6. 교육과정 변경 시 DB 수정으로 추가/삭제가 쉽게 되어야 한다.
7. 문제는 고유 problem_id로 관리한다.
8. 학생이 다시 풀 문제는 문제 자체가 아니라 problem_id를 저장한다.
9. 문제는 태블릿에 로컬 캐시한다.
10. 오프라인 학습이 가능해야 한다.
11. 학생 Stroke는 로컬 저장 후 동기화한다.
12. 서버에는 변경 데이터 중심으로 전송한다.
13. 여러 개의 나만의 해설을 저장할 수 있어야 한다.
14. 해설은 DB에서 1:N 구조로 관리한다.
15. 힌트를 별도의 DB 구조로 만든다.
16. HWPX 해설 작성이 가능해야 한다.
17. 태블릿 필기 해설 업로드가 가능해야 한다.
18. PDF 문제/해설 자동 매칭 기능을 만든다.
19. HWPX 미주 문제를 고려한다.
20. HWPX 수식과 JSON을 상호 변환하는 구조를 만든다.
21. OCR은 단일 엔진이 아니라 여러 공개 엔진을 조합한다.
22. OCR 결과를 사람이 수정할 수 있어야 한다.
23. 수정 결과를 향후 OCR 학습 데이터로 활용한다.
24. OCR 서버는 향후 학습을 통해 발전할 수 있어야 한다.
25. 권한은 마스터/선생님/학생 구조를 기본으로 한다.
26. 선생님도 문제를 업로드하고 수정할 수 있다.
27. 최종 승인은 마스터가 한다.
28. 학생은 자신의 학습 기록을 볼 수 있다.
29. 학생은 학습 진도와 앞으로의 진도를 볼 수 있다.
30. 서버 비용 절감을 기본 설계 원칙으로 한다.
31. 로컬 캐시 + 서버 동기화 + 데이터 보존 등급 + 필요한 경우에만 AI 처리하는 구조를 사용한다.
32. HWPX/PDF 원본을 가능한 한 보존한다.

---

# 47. 아직 보류하거나 추가 검토해야 할 사항

다음은 방향은 잡혀 있지만 세부 구현을 아직 확정하지 않는다.

## OCR

- 최종 OCR 엔진 조합
- 각 엔진의 정확한 역할
- 모델별 성능 비교
- GPU 서버 사양
- 학생 태블릿에서 실행할 OCR 범위
- 개인별 OCR 학습 방법

## 수식 JSON

- 최종 AST 구조
- LaTeX와의 관계
- HWPX 수식 XML과의 정확한 매핑
- 복잡한 수식 지원 범위

## 교육과정

- 실제 2022 개정 교육과정 전체 데이터 입력
- 교육과정 버전 관리 방식
- 공식 DB의 세부 구조

## 학생 분석

- 학생 성향의 초기 분석 항목
- 통계 기반 분석과 AI 분석의 경계
- 개인화 추천 알고리즘

## 서버

- 실제 서버 사양
- DB 종류
- 파일 저장소
- 동기화 프로토콜
- 백업 정책

## 보안

- 학생 개인정보
- 교사 계정
- 접근권한
- 데이터 암호화
- 학교 도입 시 필요한 보안 기준

---

# 48. 개발 순서

한 번에 전체 프로그램을 만들지 않는다.

## STEP 1 — 데이터 구조 확정

가장 먼저 한다.

```text
학생
교사
문제
교육과정
유형
공식
해설
힌트
학습기록
Stroke
파일
OCR
```

등의 DB 관계를 설계한다.

**이 단계가 매우 중요하다.**

---

## STEP 2 — HWPX/수식 프로토타입

HWPX → JSON → HWPX

그리고

JSON → 새로운 HWPX 수식 생성

을 집중적으로 검증한다.

---

## STEP 3 — OCR 실험 프로그램

실제 PDF를 넣어:

```text
영역 검출
↓
일반 OCR
↓
수학 OCR
↓
그림 영역
↓
결과 통합
```

을 테스트한다.

---

## STEP 4 — 문제 업로드 시스템

PDF/HWPX 업로드

↓

문제 분리

↓

해설 매칭

↓

메타정보 자동 추출

↓

작업자 수정

↓

마스터 승인

구조를 만든다.

---

## STEP 5 — 문제 DB

승인된 문제를 DB에 저장한다.

---

## STEP 6 — 선생님 프로그램

문제 관리/해설/힌트/교육과정/학생관리 기능을 만든다.

---

## STEP 7 — 학생 태블릿

문제 다운로드

↓

로컬 캐시

↓

오프라인 풀이

↓

Stroke 저장

↓

동기화

를 구현한다.

---

## STEP 8 — 학습 기록

학생의 문제 풀이와 진도를 기록한다.

---

## STEP 9 — OCR 학습 시스템

선생님 수정 데이터를 축적하여 OCR 개선 시스템을 만든다.

---

## STEP 10 — 개인화 AI

충분한 학습 데이터가 축적된 후 학생별 추천/성향 분석을 발전시킨다.

---

# 49. 개발 중 반드시 지켜야 할 주의사항

## 주의사항 1

**처음부터 모든 것을 완벽하게 만들려고 하지 않는다.**

대신 DB와 인터페이스의 확장성을 확보한다.

---

## 주의사항 2

**OCR 결과를 무조건 정답으로 믿지 않는다.**

AI/OCR은 항상 오류가 발생할 수 있다.

따라서 검수 및 수정 시스템이 필수다.

---

## 주의사항 3

**원본 데이터를 덮어쓰지 않는다.**

원본과 AI 결과와 사람 수정 결과를 분리한다.

---

## 주의사항 4

**학생 Stroke를 무조건 실시간 서버 전송하지 않는다.**

로컬 저장 → 압축 → 배치 동기화를 기본으로 한다.

---

## 주의사항 5

**문제를 학생별로 복제하지 않는다.**

problem_id와 로컬 캐시를 사용한다.

---

## 주의사항 6

**교육과정을 프로그램 코드에 하드코딩하지 않는다.**

DB 데이터로 관리한다.

---

## 주의사항 7

**해설 개수를 고정하지 않는다.**

1개, 2개, 10개가 되어도 DB 구조를 변경하지 않아야 한다.

---

## 주의사항 8

**학생 성향 분류를 고정하지 않는다.**

실제 데이터가 쌓이면 새로운 특성을 추가할 수 있어야 한다.

---

## 주의사항 9

**AI를 모든 작업에 사용하지 않는다.**

단순 작업은 로컬 또는 일반 서버에서 처리한다.

---

## 주의사항 10

**처음부터 학교/출판 확장을 고려한다.**

개인 학원에서 사용하는 것과 학교에서 수천~수만 명이 사용하는 것은 요구사항이 다르다.

---

# 50. 장기적인 발전 방향

최종적으로 다음과 같은 시스템을 목표로 한다.

```text
                  Mathenger
                      │
        ┌─────────────┼─────────────┐
        │             │             │
     문제 DB       교육과정 DB    학생 DB
        │             │             │
        └─────────────┼─────────────┘
                      │
                 학습 데이터
                      │
          ┌───────────┴───────────┐
          ↓                       ↓
        OCR                     AI
          ↓                       ↓
     문제 구조화               개인화
          │                       │
          └───────────┬───────────┘
                      ↓
                 학습 분석
                      ↓
                 학생별 추천
```

궁극적으로는 단순히 문제를 보여주는 프로그램이 아니라:

> **학생이 어떻게 문제를 풀고, 어디에서 막히며, 어떤 개념을 이해하지 못하고, 어떤 풀이 습관을 가지고 있는지를 데이터로 파악하고 다음 학습을 결정해주는 수학 학습 시스템**

을 목표로 한다.

---

# 51. 다른 AI가 이 프로젝트를 이어받을 때의 원칙

이 문서를 전달받은 AI/개발자는 다음을 반드시 이해해야 한다.

이 프로젝트는 단순한 OCR 프로그램이 아니다.

**핵심은 다음 네 가지가 결합된 교육 시스템이다.**

```text
① 수학 문제 데이터베이스
② 수학 전문 OCR
③ 학생 풀이/학습 데이터
④ AI 기반 개인화
```

그리고 이 네 가지를 연결하는 핵심이 **잘 설계된 데이터 구조**이다.

따라서 새로운 기능을 제안할 때는 항상:

1. 기존 DB와 어떻게 연결되는가?
2. 나중에 확장 가능한가?
3. 원본 데이터가 보존되는가?
4. 서버 비용이 불필요하게 증가하지 않는가?
5. 오프라인 환경에서 문제가 없는가?
6. 사람이 수정할 수 있는가?
7. 향후 AI 학습 데이터로 활용할 수 있는가?
8. 교육과정 변경에 대응할 수 있는가?
9. 학교/출판 확장이 가능한가?

를 먼저 검토해야 한다.

---

# 52. 현재 프로젝트의 가장 중요한 개발 원칙 한 문장

> **“처음에는 공개 기술과 단순한 구조로 시작하되, 데이터와 검수 결과가 쌓일수록 Mathenger만의 수학 데이터·OCR·학습 AI로 발전할 수 있도록 처음부터 확장 가능한 DB와 아키텍처를 만든다.”**

---

# 53. 다른 AI에게 전달할 프로젝트 시작 지시문

아래 문장을 이 문서와 함께 전달한다.

> 당신은 Mathenger 수학 교육 AI 프로젝트의 개발 파트너다.
>
> 위 문서는 지금까지 확정된 프로젝트의 개발 명세서, 결정사항, 보류사항 및 향후 계획이다.
>
> 앞으로 개발할 때 이 문서의 확정사항을 임의로 변경하지 않는다.
>
> 새로운 기술이나 구조를 제안할 때는 기존 구조와의 관계, 확장성, DB 영향, 서버 비용, 오프라인 가능성, 원본 보존, 보안 및 향후 학교/출판 확장 가능성을 함께 검토한다.
>
> 특히 DB를 먼저 안정적으로 설계한 후 기능을 개발한다.
>
> 교육과정, 문제 유형, 해설, 힌트, 학생 성향 등은 처음부터 고정된 숫자나 종류로 제한하지 않고 데이터가 축적되면서 확장될 수 있도록 설계한다.
>
> OCR은 하나의 엔진에 의존하지 않고 여러 공개 OCR/수학 OCR 모델을 조합하는 구조로 시작한다.
>
> OCR 결과는 사람이 검수하고 수정할 수 있어야 하며, 수정 결과는 향후 OCR 학습 데이터로 활용할 수 있어야 한다.
>
> 학생 태블릿은 오프라인에서도 문제 풀이와 Stroke 기록이 가능해야 하며, 서버에는 필요한 데이터만 동기화한다.
>
> 문제는 problem_id를 중심으로 관리하고 학생이 다시 풀 문제를 저장할 때 문제 자체를 복제하지 않는다.
>
> HWPX와 수식 JSON의 상호 변환은 중요한 핵심 기술이므로 개발 초기에 별도의 프로토타입으로 검증한다.
>
> 최종 목표는 단순 학원용 프로그램이 아니라 중·고등학교 부교재 및 학습 시스템으로 확장 가능한 수학 교육 플랫폼을 만드는 것이다.
>
> 따라서 단기적으로 작동하는 코드보다 장기적으로 유지·확장 가능한 구조를 우선한다.

---

# 54. 현재 개발 단계

현재는 **아이디어 구상 단계가 거의 완료되고 실제 개발 전 설계 단계로 넘어가는 시점**이다.

다음 실제 작업은:

**① DB 상세 설계 → ② HWPX/JSON 기술 검증 → ③ 다중 OCR 테스트 → ④ UI 확정 → ⑤ 초기 버전 개발**

순서로 진행하는 것을 기본으로 한다.

특히 첫 개발 전에 **DB ERD와 실제 테이블 구조를 먼저 확정하는 것**이 중요하다.